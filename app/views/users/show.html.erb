<h2><%= link_to "Home", users_path  %></h2>

<h2><%= @users.name %></h2>
<h4><%= @users.description %></h4>


<%= link_to "Edit Project", edit_user_path(@users) %>

<%= link_to "Delete Project",
            user_path(@users),
            method: :delete,
            data: { confirm:
                        "Are you sure you want to delete this project?"
            } %>